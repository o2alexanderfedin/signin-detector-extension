---
phase: 01-detection-core-engine-sensors-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - wxt.config.ts
  - vitest.config.ts
  - eslint.config.mjs
  - .gitignore
  - entrypoints/background.ts
  - entrypoints/content.ts
  - src/shared/types.ts
  - src/shared/constants.ts
  - src/shared/smoke.test.ts
autonomous: true
requirements: [ENG-01, ENG-02, ENG-03, ENG-04, ENG-05, SEN-01, SEN-02, SEN-03, SEN-04, SEN-05, IDN-01, IDN-02]

must_haves:
  truths:
    - "Running `npm test` executes the Vitest suite (happy-dom environment) and exits 0, even with only a smoke test present"
    - "Running `npm run compile` (tsc --noEmit) exits 0 with strict mode plus noUncheckedIndexedAccess and exactOptionalPropertyTypes active"
    - "Running `npm run lint` (eslint flat config, typescript-eslint type-checked rules) exits 0"
    - "The shared contracts (WebAppKey, SignalEvidence, SignalVector, VerdictState/VerdictResult, ClockFn, and the named weight/threshold/debounce/denylist constants) exist, compile, and are the exact frozen shapes Plan 02 and Plan 03 both import without modifying"
  artifacts:
    - path: "package.json"
      provides: "Pinned WXT 0.20.27 + TypeScript 6.0.3 (not 7.x) + Vitest 4.1.10 + tldts 7.4.9 toolchain"
      contains: "\"typescript\": \"6.0.3\""
    - path: "tsconfig.json"
      provides: "Strict TS config extending WXT's generated base, with noUncheckedIndexedAccess and exactOptionalPropertyTypes"
      contains: "noUncheckedIndexedAccess"
    - path: "wxt.config.ts"
      provides: "Forced MV3 manifest output for both Chrome and Firefox targets"
      contains: "manifestVersion: 3"
    - path: "vitest.config.ts"
      provides: "Vitest wired to WXT's fake-browser plugin, happy-dom as default test environment"
      contains: "WxtVitest"
    - path: "src/shared/types.ts"
      provides: "WebAppKey branded type, SignalEvidence discriminated union, SignalVector, VerdictState/VerdictResult, ClockFn, message contract types"
      min_lines: 60
    - path: "src/shared/constants.ts"
      provides: "Named signal weights, hysteresis thresholds, debounce windows, strong-negative magnitude, and per-sensor scoring/denylist constants for all four classifiers"
      min_lines: 55
  key_links:
    - from: "tsconfig.json"
      to: "./.wxt/tsconfig.json"
      via: "extends"
      pattern: "extends.*\\.wxt/tsconfig"
    - from: "vitest.config.ts"
      to: "wxt/testing/vitest-plugin"
      via: "WxtVitest() plugin registration"
      pattern: "WxtVitest"
    - from: "src/shared/constants.ts"
      to: "src/shared/types.ts"
      via: "SignalName type-only import for SIGNAL_WEIGHTS record"
      pattern: "import type \\{ SignalName \\} from '\\./types'"
---

<objective>
Scaffold the WXT + TypeScript 6.0.3 project from scratch (this repo currently has no `src/`, no `package.json` — greenfield) and write the frozen Tier-0 contracts (`src/shared/types.ts`, `src/shared/constants.ts`) that Plan 02 (identity + four classifiers) and Plan 03 (ConfidenceEngine) both build against in parallel next wave.

Purpose: everything downstream depends on this plan. The toolchain must be pinned exactly per `.planning/research/STACK.md` (TypeScript 6.0.3, NOT 7.x — a same-week landmine per research) so `tsc`/`typescript-eslint` work correctly, and the shared types/constants must be complete and stable before Plan 02/03 start, so those two plans can run in the same wave with zero file conflicts (neither modifies `src/shared/*` — they only import from it).

Output: a working `npm test` / `npm run compile` / `npm run lint` toolchain with no `src/engine`, `src/identity`, or `src/sensors` code yet (that's Plan 02/03) — just the scaffold and the shared contracts, proven to compile and to run under Vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-detection-core-engine-sensors-identity/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

This is a greenfield repo: `.planning/` and root-level docs exist, but there is no `package.json`, no `src/`, no WXT project yet. This plan creates all of it.

**IMPORTANT — do not run `npx wxt@latest init`.** The project root is a non-empty directory (`.git`, `.planning`, `CLAUDE.md`, `docs/`, etc.) and `wxt init` refuses to run in any non-empty directory with no override flag (verified live against wxt@0.20.27 during planning: `ERROR The directory ... is not empty. Aborted.`). Also note the current `wxt init --template` values are `vanilla | vue | react | solid | svelte` (no `-ts` suffix variants — `vanilla-ts` does not exist as of wxt@0.20.27, verified live against the `wxt-dev/wxt` GitHub template listing; STACK.md's installation snippet using `-t vanilla-ts` is stale). Instead, hand-write every scaffold file below with the Write tool using the exact content given — this was validated end-to-end during planning (`npm install` succeeds, `wxt prepare` succeeds, `tsc --noEmit` is clean, `npx vitest run` passes, `eslint` is clean, and `wxt build` produces a correct `manifest_version: 3` output).

**Also note:** STACK.md's testing table describes happy-dom as "bundled, Vitest default DOM env" — this is inaccurate for vitest@4.1.10; happy-dom must be installed explicitly as a devDependency (verified live: Vitest throws `Cannot find package 'happy-dom'` without it). The exact `<interfaces>` block below already accounts for this.

<interfaces>
<!-- Exact file contents to create. This is the frozen Tier-0 contract for
the whole phase. Every value below (versions, flags, exports, constants)
was verified during planning: npm packages resolve at these exact pinned
versions, wxt prepare/build succeed, tsc --noEmit is clean, and a prototype
engine + cookie classifier built against these exact types/constants passed
14 fixture-based Vitest assertions. Write each file verbatim. -->

--- package.json ---
```json
{
  "name": "sign-in-detector",
  "description": "Chrome MV3 (Firefox-compatible) extension that fuses cookie/network/storage/DOM signals into a sign-in confidence verdict, rendered as a border overlay",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "build:firefox": "wxt build -b firefox",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox",
    "compile": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@playwright/test": "1.61.1",
    "@types/chrome": "0.2.2",
    "@vitest/eslint-plugin": "^1.6.23",
    "@webext-core/fake-browser": "1.5.2",
    "eslint": "^9.39.5",
    "happy-dom": "^20.10.6",
    "jsdom": "^27.0.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.64.0",
    "vitest": "4.1.10",
    "wxt": "0.20.27"
  },
  "dependencies": {
    "@webext-core/messaging": "3.0.2",
    "tldts": "7.4.9"
  }
}
```
Note: versions with no `^`/`~` prefix are exact-pinned deliberately (matches STACK.md's exact pins, especially `typescript` — see the TS7 landmine note in STACK.md). `eslint`, `happy-dom`, `jsdom`, `@vitest/eslint-plugin`, `@eslint/js` use caret ranges since STACK.md does not exact-pin those.

--- wxt.config.ts ---
```typescript
import { defineConfig } from 'wxt';

// See https://wxt.dev/api/config.html
// manifestVersion is forced to 3 for both Chrome and Firefox build targets
// (WXT otherwise defaults Firefox output to MV2) -- this project's
// constraint is one consistent MV3 manifest across both browsers.
export default defineConfig({
  manifestVersion: 3,
});
```

--- tsconfig.json ---
```json
{
  "extends": "./.wxt/tsconfig.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM", "WebWorker"],
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true
  }
}
```
Note: `./.wxt/tsconfig.json` is generated by `wxt prepare` (runs automatically via the `postinstall` script) and already sets `strict: true`. `lib` adds `WebWorker` (service-worker globals like `self`) alongside `DOM` (content-script/happy-dom-testable code) per STACK.md's documented gap. Do not hand-edit `.wxt/tsconfig.json` itself — it is regenerated on every `wxt prepare`.

--- vitest.config.ts ---
```typescript
import { defineConfig } from 'vitest/config';
import { WxtVitest } from 'wxt/testing/vitest-plugin';

export default defineConfig({
  plugins: [WxtVitest()],
  test: {
    environment: 'happy-dom',
  },
});
```

--- eslint.config.mjs ---
```javascript
// @ts-check
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.recommendedTypeChecked,
  {
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: import.meta.dirname,
      },
    },
  },
  {
    ignores: ['.output/**', '.wxt/**', 'node_modules/**'],
  },
);
```

--- entrypoints/background.ts ---
```typescript
export default defineBackground(() => {
  // Chrome API glue (cookies/webRequest/messaging/storage) is added in
  // Phase 3-4. This entrypoint intentionally stays inert in Phase 1, which
  // is pure, Chrome-API-free detection logic only (src/shared, src/engine,
  // src/identity, src/sensors/*).
});
```

--- entrypoints/content.ts ---
```typescript
export default defineContentScript({
  matches: ['<all_urls>'],
  main() {
    // Storage/DOM sensor glue and the border overlay are added in
    // Phase 3-4. This entrypoint intentionally stays inert in Phase 1.
  },
});
```

--- .gitignore additions ---
The project already has a `.gitignore` with `.playwright-mcp/`, `node_modules/`, `dist/`, `*.log`, `*.zip`. Use the Edit tool to APPEND these lines (do not overwrite the existing file, do not duplicate `node_modules/` or `*.log` which are already present):
```
.output
.wxt
stats.html
stats-*.json
web-ext.config.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-write the WXT + TypeScript 6.0.3 scaffold and verify the toolchain</name>
  <files>package.json, tsconfig.json, wxt.config.ts, vitest.config.ts, eslint.config.mjs, .gitignore, entrypoints/background.ts, entrypoints/content.ts</files>
  <action>
Using the Write tool, create each file listed in the `<interfaces>` block above with the exact content shown (package.json, wxt.config.ts, tsconfig.json, vitest.config.ts, eslint.config.mjs, entrypoints/background.ts, entrypoints/content.ts). Then use the Edit tool to append the listed `.gitignore` additions to the existing `.gitignore` (read it first; do not duplicate existing lines).

Run `npm install` once, from the project root, with all files above already in place — this installs every pinned dependency from package.json and automatically triggers the `postinstall` script (`wxt prepare`), which generates `.wxt/tsconfig.json` and `.wxt/types/`. Do not run `wxt init` (see the non-empty-directory / stale-template-flag note in context above). Do not manually create `.wxt/` — it is fully generated by `wxt prepare`.

If `npm install` reports vulnerabilities via `npm audit`, do not run `npm audit fix --force` — this can silently bump `typescript` past 6.x and reintroduce the TS7 landmine documented in STACK.md. Leave audit warnings as-is for this phase.
  </action>
  <verify>
    <automated>npm run compile && npm run lint</automated>
  </verify>
  <done>`.wxt/tsconfig.json` exists (generated). `npm run compile` (tsc --noEmit) exits 0 with `noUncheckedIndexedAccess` and `exactOptionalPropertyTypes` active. `npm run lint` exits 0. `package.json` devDependencies show `"typescript": "6.0.3"` exactly (no caret). No `entrypoints/popup/` exists (popup UI is explicitly out of scope per PROJECT.md/REQUIREMENTS.md — this scaffold never creates one).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the frozen shared contracts (types.ts, constants.ts) and prove the test harness works</name>
  <files>src/shared/types.ts, src/shared/constants.ts, src/shared/smoke.test.ts</files>
  <action>
Create `src/shared/types.ts` with exactly these exports (this is the exact, planning-verified contract — do not rename, restructure, or add fields; Plan 02 and Plan 03 are written against this precise shape):
- `WebAppKey` — `type WebAppKey = string & { readonly __brand: 'WebAppKey' }` (registrable-domain branded type, per IDN-02).
- `SignalName` — `'cookie' | 'network' | 'storage' | 'dom'`.
- `VerdictState` — `'unknown' | 'signed-in' | 'signed-out'`.
- `SignalEvidence` — a discriminated union keyed on `signal` and `observed`: for `cookie`/`network`/`storage`, the observed variant is `{signal, observed: true, value: number}`; for `dom`, the observed variant additionally carries `passwordFormVisible: boolean` (needed by the engine's ENG-02 cross-signal rule — a DOM-only classifier cannot know whether a session cookie exists, so it must expose this boolean and let the engine combine it with cookie evidence). All four signals have an `{signal, observed: false}` variant with no other fields. All fields `readonly`.
- `SignalVector` — `{readonly [K in SignalName]?: SignalEvidence}`, a snapshot of currently-known evidence per signal for one WebAppKey/tab.
- `VerdictResult` — `{readonly state: VerdictState; readonly confidence: number}`.
- `ClockFn` — `() => number`, an injectable clock so ENG-04's debounce timing is deterministically testable; document inline that engine logic must never call `Date.now()` directly, only through a `ClockFn` parameter.
- Message contract types (typed now as the shared seam Phase 3/4 will wire up; booleans/enums/numbers only, never a raw cookie/token field): `SensorSignalMessage {type: 'SENSOR_SIGNAL', signal: Extract<SignalName, 'storage'|'dom'>, evidence: SignalEvidence}`, `GetVerdictMessage {type: 'GET_VERDICT'}`, `VerdictUpdateMessage {type: 'VERDICT_UPDATE', result: VerdictResult}`, `ExtensionMessage = SensorSignalMessage | GetVerdictMessage`.

Create `src/shared/constants.ts` importing `type { SignalName }` from `./types`, with exactly these named exports (no magic numbers elsewhere in the codebase — every classifier/engine constant lives here):
- Signal weights (ENG-01): `SIGNAL_WEIGHT_COOKIE = 1.0`, `SIGNAL_WEIGHT_NETWORK = 1.0`, `SIGNAL_WEIGHT_STORAGE = 0.6`, `SIGNAL_WEIGHT_DOM = 0.3`, and `SIGNAL_WEIGHTS: Readonly<Record<SignalName, number>>` built from those four.
- Hysteresis thresholds (ENG-03): `THRESHOLD_SIGNED_IN = 0.7`, `THRESHOLD_SIGNED_OUT = 0.3`.
- Asymmetric debounce (ENG-04): `DEBOUNCE_SIGNED_IN_MS = 0`, `DEBOUNCE_SIGNED_OUT_MS = 3000`.
- Strong-negative magnitude (ENG-02): `STRONG_NEGATIVE_VALUE = -1` — reused by both the network classifier's 401/403 case and the engine's password-form+no-cookie cross-signal override.
- Cookie sensor (SEN-01/02): `COOKIE_MIN_HIGH_ENTROPY_LENGTH = 24`, `COOKIE_MIN_LONG_EXPIRY_MS = 24*60*60*1000`, `COOKIE_POSITIVE_VALUE = 1`, `COOKIE_PARTIAL_VALUE = 0.5`, `COOKIE_DENYLISTED_VALUE = 0.1`, `TRACKING_COOKIE_NAME_PATTERNS: readonly RegExp[]` covering `_ga`, `_gid`, `_gat`, `_fbp`, `_fbc`, `OptanonConsent`, `OptanonAlertBoxClosed`, `__utm*`, `_hjid`, `_hjSession` (case-insensitive).
- Network sensor (SEN-03/ENG-05): `IDENTITY_ENDPOINT_PATH_PATTERNS: readonly RegExp[]` matching `/me`, `/session`, `/account`, `/user` (word-boundary, case-insensitive), `GRAPHQL_ENDPOINT_PATH_PATTERN = /\/graphql\b/i`, `NETWORK_REST_IDENTITY_200_VALUE = 1`, `NETWORK_GRAPHQL_200_VALUE = 0.5` (down-weighted: a GraphQL 200 cannot be distinguished from a "soft-200" auth-error shape without body parsing, which is explicitly out of scope per REQUIREMENTS.md — document this reasoning as a comment).
- Storage sensor (SEN-04/ENG-05): `JWT_SEGMENT_COUNT = 3`, `STORAGE_KEY_NAME_PATTERNS: readonly RegExp[]` matching `auth`, `session`, `token` (case-insensitive), `STORAGE_KEY_NAME_ONLY_VALUE = 0.5`, `STORAGE_POSITIVE_VALUE = 1`, `STORAGE_GUEST_OR_STALE_VALUE = 0.1`, `GUEST_CLAIM_KEYS: readonly string[]` = `['role','scope','type','user_type']`, `GUEST_CLAIM_VALUE_PATTERN = /guest|anonymous|^anon$/i`.
- DOM sensor (SEN-05): `DOM_POSITIVE_VALUE = 0.6`, `DOM_AMBIGUOUS_VALUE = 0.2`.

Create `src/shared/smoke.test.ts` with a single Vitest test proving the harness works end-to-end: assert `typeof document === 'object'` (confirms happy-dom is active) and one trivial arithmetic assertion.

This task does not need `tdd="true"`/a `<behavior>` block — these are type/constant declarations with no branching logic to red-green-refactor; the smoke test is the harness proof, not a spec for this module's behavior.
  </action>
  <verify>
    <automated>npm test && npm run compile</automated>
  </verify>
  <done>`npm test` passes (at least the smoke test, exit 0). `npm run compile` exits 0. `src/shared/types.ts` and `src/shared/constants.ts` export exactly the names listed above (verify with `grep -c '^export' src/shared/types.ts` and the same for constants.ts — both should be non-trivial counts, not empty stubs).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> project dependencies | Third-party packages pulled into the build/runtime toolchain during `npm install` |
| package.json `postinstall` hook -> local script execution | `npm install` automatically executes `wxt prepare` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-01 | Tampering | package.json dependency resolution | mitigate | Exact-pin every STACK.md-specified package (no caret) so `npm install` cannot silently drift to an incompatible/untested version (e.g. TypeScript 7.x, which STACK.md documents as a same-week breaking landmine); versions verified installable against the live npm registry during planning (2026-07-17) |
| T-01-02 | Elevation of Privilege | `postinstall` script (`wxt prepare`) | accept | `wxt prepare` is WXT's own first-party type-generation step, not third-party code; no additional untrusted script executes via `postinstall` beyond the already-vetted `wxt` dependency |
| T-01-03 | Information Disclosure | Checked-in config files (tsconfig.json, eslint.config.mjs, wxt.config.ts) | accept | No secrets or credentials in any scaffold file; all values are public build configuration |
</threat_model>

<verification>
- `npm run compile` (tsc --noEmit) exits 0.
- `npm run lint` (eslint .) exits 0.
- `npm test` (vitest run) exits 0 with at least one passing test.
- `package.json` devDependencies contains `"typescript": "6.0.3"` (exact, no `^`).
- `src/shared/types.ts` and `src/shared/constants.ts` exist and export the exact names specified in Task 2.
</verification>

<success_criteria>
A fresh clone of the repo can run `npm install && npm test && npm run compile && npm run lint` and all four succeed, with `src/shared/types.ts` and `src/shared/constants.ts` providing every type and named constant Plan 02 and Plan 03 need, without either of those plans needing to modify `src/shared/*`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-detection-core-engine-sensors-identity/01-01-SUMMARY.md`
</output>
